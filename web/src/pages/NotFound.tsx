'use client'

import {
    Box,
} from '@chakra-ui/react'

export default function NotFound() {
    return (
        <Box maxW="7xl" mx={'auto'} pt={5} px={{ base: 2, sm: 12, md: 17 }}>
        </Box>
    )
}